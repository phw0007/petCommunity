<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="com.care.project.ashop.AShopMapper">
	<resultMap type="com.care.project.ashop.AShopDTO" id="ashopDto">
		<id column="no" property="no"/>
		<result column="category" property="category"/>
		<result column="name" property="name"/>
		<result column="company" property="company"/>
		<result column="pay" property="pay"/>
		<result column="inventory" property="inventory"/>
		<result column="image_file" property="imageFile"/>
		<result column="info" property="info"/>
	</resultMap>
	
		<select id="shopData" resultMap="ashopDto">
		<choose>
			<when test="select == 'category'">
				SELECT AAA.* FROM 
				(SELECT rownum as rn, no, category, name, company, pay, inventory
				, image_file, info FROM project_shop WHERE category like ('%' ||  #{search} || '%'))AAA 
				WHERE AAA.rn BETWEEN #{begin} AND #{end}
			</when>
			<when test="select == 'company'">
				SELECT AAA.* FROM 
				(SELECT rownum as rn, no, category, name, company, pay, inventory
				, image_file, info FROM project_shop WHERE company like ('%' ||  #{search} || '%'))AAA 
				WHERE AAA.rn BETWEEN #{begin} AND #{end}
			</when>
			<when test="select == 'name'">
				SELECT AAA.* FROM 
				(SELECT rownum as rn, no, category, name, company, pay, inventory
				, image_file, info FROM project_shop WHERE name like ('%' ||  #{search} || '%'))AAA 
				WHERE AAA.rn BETWEEN #{begin} AND #{end}
			</when>
			<otherwise>
				SELECT AAA.* FROM 
				(SELECT rownum as rn, no, category, name, company, pay, inventory
				, image_file, info FROM project_shop WHERE category like ('%' ||  #{search} || '%') or
				company like ('%' ||  #{search} || '%') or name like ('%' ||  #{search} || '%'))AAA 
				WHERE AAA.rn BETWEEN #{begin} AND #{end}
			</otherwise>
		</choose>
	</select>
	
	<select id="count">
		<choose>
	  		<when test="select == 'category'">
	  			SELECT count(name) FROM project_shop WHERE category like ('%' ||  #{search} || '%')
	  		</when>
	  		<when test="select == 'company'">
	  			SELECT count(name) FROM project_shop WHERE company like ('%' ||  #{search} || '%')
	  		</when>
	  		<when test="select == 'name'">
	  			SELECT count(name) FROM project_shop WHERE name like ('%' ||  #{search} || '%')
	  		</when>
	  		<otherwise>
	  			SELECT count(name) FROM project_shop WHERE category like ('%' ||  #{search} || '%') or
	  			company like ('%' ||  #{search} || '%') or name like ('%' ||  #{search} || '%')
	  		</otherwise>
		</choose>
	</select>
	
	<insert id="insertShop">
		<selectKey order="BEFORE" keyProperty="no" resultType="int">
			SELECT nvl(max(no),0) + 1 FROM project_shop
		</selectKey>
		INSERT INTO project_shop VALUES(#{no}, #{category}, #{name}, #{company}, #{pay}, #{inventory}, #{imageFile}, #{info})
	</insert>
	
	<select id="checkShop" resultMap="ashopDto">
		SELECT * FROM project_shop WHERE name=#{name}
	</select>
	
</mapper>







  