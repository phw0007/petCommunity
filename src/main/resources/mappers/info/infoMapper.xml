<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="com.care.project.ainfo.AInfoMapper">
	<resultMap type="com.care.project.ainfo.AInfoDTO" id="InfoDTO">
		<id column="no" property="no"/>
		<result column="category" property="category"/>
		<result column="name" property="name"/>
		<result column="email" property="email"/>
		<result column="address" property="address"/>
		<result column="mobile" property="mobile"/>
	</resultMap>

	<select id="AInfoData" resultMap="InfoDTO">
		<choose>
			<when test="select == 'category'">
				SELECT AAA.* FROM 
				(SELECT rownum as rn, no, category, name, email, address, mobile 
				FROM project_info WHERE category like ('%' ||  #{search} || '%'))AAA 
				WHERE AAA.rn BETWEEN #{begin} AND #{end}
			</when>
			<when test="select == 'name'">
				SELECT AAA.* FROM 
				(SELECT rownum as rn, no, category, name, email, address, mobile 
				FROM project_info WHERE name like ('%' ||  #{search} || '%'))AAA 
				WHERE AAA.rn BETWEEN #{begin} AND #{end}
			</when>
			<when test="select == 'address'">
				SELECT AAA.* FROM 
				(SELECT rownum as rn, no, category, name, email, address, mobile 
				FROM project_info WHERE address like ('%' ||  #{search} || '%'))AAA 
				WHERE AAA.rn BETWEEN #{begin} AND #{end}
			</when>
			<when test="select == 'mobile'">
				SELECT AAA.* FROM 
				(SELECT rownum as rn, no, category, name, email, address, mobile 
				FROM project_info WHERE mobile like ('%' ||  #{search} || '%'))AAA 
				WHERE AAA.rn BETWEEN #{begin} AND #{end}
			</when>
			<otherwise>
				SELECT AAA.* FROM 
				(SELECT rownum as rn, no, category, name, email, address, mobile
				FROM project_info WHERE category like ('%' ||  #{search} || '%') or name like ('%' ||  #{search} || '%') or
				address like ('%' ||  #{search} || '%') or mobile like ('%' ||  #{search} || '%'))AAA 
				WHERE AAA.rn BETWEEN #{begin} AND #{end}
			</otherwise>
		</choose>
	</select>
	
	<select id="count">
		<choose>
			<when test="select == 'category'">
	  			SELECT count(name) FROM project_info WHERE category like ('%' ||  #{search} || '%')
	  		</when>
	  		<when test="select == 'name'">
	  			SELECT count(name) FROM project_info WHERE name like ('%' ||  #{search} || '%')
	  		</when>
	  		<when test="select == 'address'">
	  			SELECT count(name) FROM project_info WHERE address like ('%' ||  #{search} || '%')
	  		</when>
	  		<when test="select == 'mobile'">
	  			SELECT count(name) FROM project_info WHERE mobile like ('%' ||  #{search} || '%')
	  		</when>
	  		<otherwise>
	  			SELECT count(name) FROM project_info WHERE category like ('%' ||  #{search} || '%') or
	  			name like ('%' ||  #{search} || '%') or address like ('%' ||  #{search} || '%') or 
	  			mobile like ('%' ||  #{search} || '%')
	  		</otherwise>
		</choose>
	</select>
	
</mapper>







  