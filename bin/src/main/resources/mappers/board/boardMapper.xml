<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="com.care.project.board.BoardMapper">
  	<resultMap type="com.care.project.board.BoardDTO" id="boardDto">
		<id column="no" property="no"/>
		<result column="id" property="id"/>
		<result column="category" property="category"/>
		<result column="title" property="title"/>
		<result column="content" property="content"/>
		<result column="file_name" property="fileName"/>
		<result column="write_date" property="writeDate"/>
		<result column="hits" property="hits"/>
		<result column="like CONCATs" property="like CONCATs"/>
		<result column="comment_content" property="commentContent"/>
		<result column="like CONCATno" property="like CONCATno"/>
		<result column="like CONCAT_check" property="like CONCATCheck"/>
		<result column="like CONCATs_id" property="like CONCATsId"/>
	</resultMap>
    
    <insert id="boardWriteProc">
		<selectKey order="BEFORE" keyProperty="no" resultType="int">
			SELECT nvl(max(no),0) + 1 FROM project_board
		</selectKey>
		INSERT INTO project_board VALUES(#{no},#{id}, #{category}, #{title}, #{content},#{fileName}, #{writeDate}, 0,0  )
	</insert>

	<select id="count">
	  	SELECT count(no) FROM project_board
	</select>
	
	<select id="freecount">
	  	SELECT count(no) FROM project_board where category='자유게시판'
	</select>
 
 
	<select id="freeboardForm" resultMap="boardDto">
	SELECT freeboard. * FROM 
	(SELECT ROWNUM() as rn, free. * FROM (select no,title,id,category,write_date,hits,like CONCATs from project_board where category='자유게시판' order by no desc)free)
	freeboard WHERE freeboard.rn BETWEEN #{begin} AND #{end}
	</select>


	<select id="boardContent" resultMap="boardDto">
		SELECT * FROM project_board WHERE no=#{no}
	</select>
	
	<update id = "incHit">
		UPDATE project_board SET hits=hits+1 WHERE no = #{no}
	</update>
	
	<select id = "boardDownload">
		SELECT file_name FROM project_board WHERE no = #{no}
	</select>

	
	<delete id = "boardDeleteProc">
		DELETE FROM project_board WHERE no = #{no}
	</delete>
			
	<update id = "boardModifyProc">
		UPDATE project_board SET title = #{title}, content = #{content} WHERE no = #{no}
	</update>

    <insert id="freecommentProc">
      INSERT INTO project_board_comment VALUES(#{id}, #{no}, #{category}, #{writeDate}, #{commentContent})
    </insert>
    
      <select id="freeComment" resultMap="boardDto">
		select no,id,write_date,comment_content from project_board_comment where category=#{category} AND no=#{no} order by write_date desc
	</select>
	
	<delete id="commentDeleteProc">
	   DELETE FROM project_board_comment WHERE id=#{id} AND category=#{category} AND no=#{no} AND write_date=#{writeDate}
	</delete>
	
	   <select id = "boardlike CONCATCheck" resultMap="boardDto">
      SELECT * FROM (SELECT * FROM project_board_like CONCATs 
      WHERE id=#{id} AND category=#{category} AND no=#{no})WHERE like CONCATs_id=#{like CONCATsId}
   </select>
   
   <update id = "boardHitDown">
      UPDATE project_board SET hits=hits-1 WHERE id=#{id} AND category=#{category} AND no=#{no}
   </update>
   
   <insert id="boardlike CONCATUserInsert">
      INSERT INTO project_board_like CONCATs VALUES(#{id}, #{no}, #{category}, #{like CONCATsId})
   </insert>
   
   <update id = "boardlike CONCAT">
      UPDATE project_board SET like CONCATs=like CONCATs+1 WHERE id=#{id} AND category=#{category} AND no=#{no}
   </update>
	
	<select id="boardSearch" resultMap="boardDto">
		<choose>
			<when test="select == 'title'">
	SELECT BBB.* FROM ( SELECT ROWNUM() as rn, AAA.* FROM
       (SELECT no, id, category, title, content, file_name, write_date, hits, like CONCATs FROM project_board WHERE title like CONCAT ('%' , #{search} , '%')
        ORDER BY no DESC) AAA) BBB
WHERE BBB.rn BETWEEN #{begin} AND #{end}
			</when>
			<when test="select == 'id'">
				SELECT AAA.* FROM 
				(SELECT ROWNUM() as rn, no, id, category, title, content, file_name
				, write_date, hits, like CONCATs FROM project_board WHERE id like CONCAT ('%' ,  #{search} , '%'))AAA 
				WHERE AAA.rn BETWEEN #{begin} AND #{end}
			</when>
				<otherwise>
				SELECT AAA.* FROM 
				(SELECT ROWNUM() as rn, no, id, category, title, content, file_name
				, write_date, hits, like CONCATs FROM project_board WHERE category like CONCAT ('%' ,  #{search} , '%') or
				title like CONCAT ('%' ,  #{search} , '%') or id like CONCAT ('%' ,  #{search} , '%'))AAA 
				WHERE AAA.rn BETWEEN #{begin} AND #{end}
			</otherwise>
		</choose>
	</select>
	
	<select id="searchCount">
		<choose>
	  		<when test="select == 'title'">
	  			SELECT count(no) FROM project_board WHERE title like CONCAT ('%' ,  #{search} , '%')
	  		</when>
	  		<when test="select == 'id'">
	  			SELECT count(no) FROM project_board WHERE category='자유게시판' AND id like CONCAT ('%' ,  #{search} , '%')
	  		</when>
		</choose>
	</select>
	
</mapper>


